<UserControl x:Class="HospitalManagementSystem.NurseScheduleAppointment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:HospitalManagementSystem"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
                    <Border DockPanel.Dock="Top"
                            HorizontalAlignment="Left"
                            Background="{DynamicResource MaterialDesign.Brush.Surface}"
                            BorderBrush="{DynamicResource MaterialDesign.Brush.Divider}"
                            BorderThickness="0,0,0,1" Padding="8,6">
                        <Button Style="{DynamicResource MaterialDesignFlatButton}"
                                Click="GoToDashboard_Click">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ArrowLeft" Width="18" Height="18" Margin="0,0,8,0"/>
                                <TextBlock Text="Back to Appointments"/>
                            </StackPanel>
                        </Button>
                    </Border>

                    <materialDesign:Card Margin="10"
                                         Padding="20" UniformCornerRadius="12"
                                         materialDesign:ElevationAssist.Elevation="Dp8"
                                         ClipToBounds="True">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.ColumnSpan="2" Text="Schedule Appointment"
                                           FontSize="16" FontWeight="SemiBold" Margin="0,0,0,6"/>

                                <ComboBox Grid.Row="1" Grid.ColumnSpan="2"
                                          IsEditable="True" StaysOpenOnEdit="True"
                                          Style="{DynamicResource MaterialDesignOutlinedComboBox}"
                                          materialDesign:HintAssist.Hint="Patient"
                                          ItemsSource="{Binding Patients}"
                                          Text="{Binding NewAppointment.PatientName, Mode=TwoWay}"/>

                                <ComboBox Grid.Row="2" Grid.ColumnSpan="2"
                                          Style="{DynamicResource MaterialDesignOutlinedComboBox}"
                                          materialDesign:HintAssist.Hint="Doctor"
                                          ItemsSource="{Binding Doctors}"
                                          SelectedItem="{Binding NewAppointment.DoctorName}"/>

                                <Grid Grid.Row="3" Grid.ColumnSpan="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <DatePicker Grid.Column="0"
                                                Style="{DynamicResource MaterialDesignFilledDatePicker}"
                                                materialDesign:HintAssist.Hint="Date"
                                                SelectedDate="{Binding NewAppointment.Date}"/>
                                    <TextBox Grid.Column="1"
                                             Style="{DynamicResource MaterialDesignFilledTextBox}"
                                             materialDesign:HintAssist.Hint="Time (HH:mm)"
                                             Text="{Binding NewAppointment.Time}"/>
                                </Grid>

                                <TextBox Grid.Row="4" Grid.ColumnSpan="2"
                                         AcceptsReturn="True" TextWrapping="Wrap"
                                         VerticalAlignment="Stretch" MinHeight="140"
                                         Style="{DynamicResource MaterialDesignFilledTextBox}"
                                         materialDesign:HintAssist.Hint="Reason / Notes"
                                         Text="{Binding NewAppointment.Notes}"/>

                                <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="Cancel" Style="{DynamicResource MaterialDesignFlatButton}"/>
                                    <Button Content="Create" Style="{DynamicResource MaterialDesignRaisedButton}" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                    </materialDesign:Card>
                </DockPanel>
</UserControl>
