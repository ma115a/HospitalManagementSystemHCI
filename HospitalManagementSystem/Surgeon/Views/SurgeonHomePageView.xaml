<UserControl x:Class="HospitalManagementSystem.Surgeon.Views.SurgeonHomePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:HospitalManagementSystem.Surgeon"
             xmlns:loc="clr-namespace:HospitalManagementSystem.Utils"
             mc:Ignorable="d"
             Background="{DynamicResource MaterialDesignPaper}"
             >
    <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        

                        <UniformGrid Grid.Row="1" Columns="3">
                            <UniformGrid.Resources>
                                <Style TargetType="materialDesign:Card">
                                    <Setter Property="Padding" Value="20"/>
                                    <Setter Property="UniformCornerRadius" Value="12"/>
                                    <Setter Property="Margin" Value="12"/>
                                    <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp8"/>
                                </Style>
                            </UniformGrid.Resources>

                            <!-- My Surgeries -->
                            <materialDesign:Card>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="CalendarClock" Width="32" Height="32"/>
                                        <TextBlock Text="{loc:Loc mySurgeries}" Margin="10,0,0,0"
                                                   FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock Grid.Row="1" Margin="0,12,0,0" Opacity=".9"
                                               Text="{loc:Loc mySurgeriesContent}"
                                               TextWrapping="Wrap"/>
                                    <Button Grid.Row="2" Content="{loc:Loc adminCardButton}" Width="140"
                                            Style="{DynamicResource MaterialDesignRaisedLightButton}"
                                            
                                            Command="{Binding DataContext.GoToSurgeriesCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            />
                                </Grid>
                            </materialDesign:Card>

                            <!-- Schedule Surgery -->
                            <materialDesign:Card>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="CalendarPlus" Width="32" Height="32"/>
                                        <TextBlock Text="{loc:Loc scheduleSurgery}" Margin="10,0,0,0"
                                                   FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock Grid.Row="1" Margin="0,12,0,0" Opacity=".9"
                                               Text="{loc:Loc scheduleSurgeryContent}"
                                               TextWrapping="Wrap"/>
                                    <Button Grid.Row="2" Content="{loc:Loc adminCardButton}" Width="160"
                                            Style="{DynamicResource MaterialDesignRaisedLightButton}"
                                            Command="{Binding DataContext.GoToScheduleSurgeryCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            />
                                </Grid>
                            </materialDesign:Card>

                            <!-- History -->
                            <!-- <materialDesign:Card> -->
                            <!--     <Grid> -->
                            <!--         <Grid.RowDefinitions> -->
                            <!--             <RowDefinition Height="Auto"/> -->
                            <!--             <RowDefinition Height="*"/> -->
                            <!--             <RowDefinition Height="Auto"/> -->
                            <!--         </Grid.RowDefinitions> -->
                            <!--         <StackPanel Orientation="Horizontal"> -->
                            <!--             <materialDesign:PackIcon Kind="History" Width="32" Height="32"/> -->
                            <!--             <TextBlock Text="{loc:Loc surgeryHistory}" Margin="10,0,0,0" -->
                            <!--                        FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/> -->
                            <!--         </StackPanel> -->
                            <!--         <TextBlock Grid.Row="1" Margin="0,12,0,0" Opacity=".9" -->
                            <!--                    Text="{loc:Loc surgeryHistoryContent}" -->
                            <!--                    TextWrapping="Wrap"/> -->
                            <!--         <Button Grid.Row="2" Content="{loc:Loc adminCardButton}" Width="140" -->
                            <!--                 Style="{DynamicResource MaterialDesignRaisedLightButton}" -->
                            <!--                 Command="{Binding DataContext.GoToHistoryCommand, RelativeSource={RelativeSource AncestorType=Window}}" -->
                            <!--                 /> -->
                            <!--     </Grid> -->
                            <!-- </materialDesign:Card> -->
                        </UniformGrid>
                    </Grid>
                </ScrollViewer>
</UserControl>
