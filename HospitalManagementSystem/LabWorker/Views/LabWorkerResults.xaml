<UserControl x:Class="HospitalManagementSystem.LabWorker.Views.LabWorkerResults"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:loc="clr-namespace:HospitalManagementSystem.Utils"
             xmlns:local="clr-namespace:HospitalManagementSystem"
             Background="{DynamicResource MaterialDesignPaper}"
             d:DesignHeight="1200
             "
             mc:Ignorable="d"
             >
    <DockPanel>
                    <Border DockPanel.Dock="Top"
                            HorizontalAlignment="Left"
                            Background="{DynamicResource MaterialDesign.Brush.Surface}"
                            BorderBrush="{DynamicResource MaterialDesign.Brush.Divider}"
                            BorderThickness="0,0,0,1" Padding="8,6">
                        <Button Style="{DynamicResource MaterialDesignFlatButton}"
                                Click="GoToDashboard_Click">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ArrowLeft" Width="18" Height="18" Margin="0,0,8,0"/>
                                <TextBlock Text="{loc:Loc backToDashboard}"/>
                            </StackPanel>
                        </Button>
                    </Border>

                    <materialDesign:Card Margin="10"
                                         Padding="20" UniformCornerRadius="12"
                                         materialDesign:ElevationAssist.Elevation="Dp8"
                                         >
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>  <!-- title -->
                                    <RowDefinition Height="Auto"/>  <!-- pick request -->
                                    <RowDefinition Height="Auto"/>  <!-- date -->
                                    <RowDefinition Height="Auto"/>     <!-- result data -->
                                    <RowDefinition Height="*"/>     <!-- result data -->
                                    <RowDefinition Height="Auto"/>  <!-- buttons -->
                                </Grid.RowDefinitions>

                                <TextBlock Grid.ColumnSpan="2" Text="{loc:Loc enterResults}"
                                           FontSize="16" FontWeight="SemiBold" Margin="0,0,0,6"/>

                                <!-- Choose request to attach the result to -->
                                <ComboBox Grid.Row="1" Grid.ColumnSpan="2"
                                          
                                          Margin="0,0,0,10"
                                          ItemsSource="{Binding LaboratoryTests}"
                                          Style="{DynamicResource MaterialDesignOutlinedComboBox}"
                                          SelectedItem="{Binding SelectedLaboratoryTest}"
                                          materialDesign:HintAssist.IsFloating="False"
                                          materialDesign:HintAssist.Hint="{loc:Loc selectRequest}">
                                    <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding name}"></TextBlock>
                                            <TextBlock Text=" - "></TextBlock>
                                            <TextBlock Text="{Binding patient_umcnNavigation.name}"></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                                </ComboBox>

                                <!-- Result date -->
                                <DatePicker Grid.Row="2" Grid.ColumnSpan="1"
                                            Style="{DynamicResource MaterialDesignFilledDatePicker}"
                                            materialDesign:HintAssist.Hint="{loc:Loc date}"
                                            materialDesign:TextFieldAssist.TextFieldCornerRadius="6" 
                                            materialDesign:TextFieldAssist.HasOutlinedTextField="True"
                                            materialDesign:TextFieldAssist.RippleOnFocusEnabled="True"
                                            materialDesign:HintAssist.IsFloating="False"
                                            Margin="0,0,5,10"
                                            IsEnabled="{Binding IsControlsEnabled}"
                                            SelectedDate="{Binding SelectedLaboratoryTestDate}"/>
                                
                                <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="1"
                                         Style="{DynamicResource MaterialDesignFilledTextBox}"
                                         materialDesign:HintAssist.Hint="{loc:Loc time}"
                                         materialDesign:TextFieldAssist.TextFieldCornerRadius="6" 
                                         materialDesign:TextFieldAssist.HasOutlinedTextField="True"
                                         materialDesign:TextFieldAssist.RippleOnFocusEnabled="True"
                                         materialDesign:HintAssist.IsFloating="False"
                                         Margin="5, 0, 0, 10"
                                         IsEnabled="{Binding IsControlsEnabled}"
                                         PreviewTextInput="TimeBox_PreviewTextInput"
                                         TextChanged="TimeBox_TextChanged"
                                         PreviewKeyDown="TimeBox_PreviewKeyDown"
                                         Text="{Binding SelectedLaboratoryTestTime}"/>
                                
                                <ComboBox Grid.Row="3" Grid.ColumnSpan="2"
                                          Style="{DynamicResource MaterialDesignOutlinedComboBox}"
                                          materialDesign:HintAssist.Hint="Status"
                                          materialDesign:TextFieldAssist.TextFieldCornerRadius="6" 
                                          materialDesign:TextFieldAssist.HasOutlinedTextField="True"
                                          materialDesign:TextFieldAssist.RippleOnFocusEnabled="True"
                                          materialDesign:HintAssist.IsFloating="False"
                                          Margin="0,0,0,10"
                                          SelectedValuePath="Content"
                                          
                                          SelectedValue="{Binding SelectedLaboratoryTest.status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"                                             IsEnabled="{Binding IsControlsEnabled}"
                                        >
                                    <ComboBoxItem>Scheduled</ComboBoxItem>
                                    <ComboBoxItem>Completed</ComboBoxItem>
                                    <ComboBoxItem>Cancelled</ComboBoxItem>
                                </ComboBox>
                                
                                <!-- Result text -->
                                <TextBox Grid.Row="4" Grid.ColumnSpan="2"
                                         AcceptsReturn="True" TextWrapping="Wrap"
                                         VerticalAlignment="Stretch" MinHeight="220"
                                         Style="{DynamicResource MaterialDesignFilledTextBox}"
                                         materialDesign:TextFieldAssist.TextFieldCornerRadius="6" 
                                         materialDesign:TextFieldAssist.HasOutlinedTextField="True"
                                         materialDesign:TextFieldAssist.RippleOnFocusEnabled="True"
                                         materialDesign:HintAssist.IsFloating="False"
                                         materialDesign:HintAssist.Hint="{loc:Loc resultData}"
                                         Margin="0,0,0,10"
                                         IsEnabled="{Binding IsControlsEnabled}"
                                         Text="{Binding LaboratoryTestResult.result_data}"/>

                                <materialDesign:Snackbar Grid.Row="5" Grid.Column="0" 
                                                         MessageQueue="{Binding MessageQueue}"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Bottom"
                                                         HorizontalContentAlignment="Center"
                                                         Padding="0"
                                         
                                                         Background="{DynamicResource MaterialDesignPaper}"
                                                         Foreground="{DynamicResource MaterialDesignBody}"
                                >
                                    <materialDesign:Snackbar.Resources>
                                        <Style TargetType="ContentPresenter">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                        </Style>
                                    </materialDesign:Snackbar.Resources>
                                </materialDesign:Snackbar>
                                <StackPanel Grid.Row="5" Grid.ColumnSpan="2"
                                            Margin="0,0,0,10"
                                            Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="{loc:Loc cancelBtn}" IsEnabled="{Binding IsControlsEnabled}" Style="{DynamicResource MaterialDesignFlatButton}" Command="{Binding CancelResultCommand}"/>
                                    <Button Content="{loc:Loc saveBtn}" IsEnabled="{Binding IsControlsEnabled}" Style="{DynamicResource MaterialDesignRaisedButton}" Margin="8,0,0,0"
                                            Command="{Binding SaveResultCommand}"/>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                    </materialDesign:Card>
                </DockPanel>
</UserControl>
