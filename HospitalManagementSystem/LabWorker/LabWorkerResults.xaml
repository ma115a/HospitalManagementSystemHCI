<UserControl x:Class="HospitalManagementSystem.LabWorkerResults"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:HospitalManagementSystem"
             mc:Ignorable="d"
             >
    <DockPanel>
                    <Border DockPanel.Dock="Top"
                            HorizontalAlignment="Left"
                            Background="{DynamicResource MaterialDesign.Brush.Surface}"
                            BorderBrush="{DynamicResource MaterialDesign.Brush.Divider}"
                            BorderThickness="0,0,0,1" Padding="8,6">
                        <Button Style="{DynamicResource MaterialDesignFlatButton}"
                                Click="GoToDashboard_Click">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ArrowLeft" Width="18" Height="18" Margin="0,0,8,0"/>
                                <TextBlock Text="Back to Requests"/>
                            </StackPanel>
                        </Button>
                    </Border>

                    <materialDesign:Card Margin="10"
                                         Padding="20" UniformCornerRadius="12"
                                         materialDesign:ElevationAssist.Elevation="Dp8"
                                         ClipToBounds="True">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>  <!-- title -->
                                    <RowDefinition Height="Auto"/>  <!-- pick request -->
                                    <RowDefinition Height="Auto"/>  <!-- date -->
                                    <RowDefinition Height="*"/>     <!-- result data -->
                                    <RowDefinition Height="Auto"/>  <!-- buttons -->
                                </Grid.RowDefinitions>

                                <TextBlock Grid.ColumnSpan="2" Text="Enter Result"
                                           FontSize="16" FontWeight="SemiBold" Margin="0,0,0,6"/>

                                <!-- Choose request to attach the result to -->
                                <ComboBox Grid.Row="1" Grid.ColumnSpan="2"
                                          IsEditable="True" StaysOpenOnEdit="True" IsTextSearchEnabled="False"
                                          ItemsSource="{Binding LabRequestsOpen}"
                                          Text="{Binding ResultEntry.SelectedRequestDisplay, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Style="{DynamicResource MaterialDesignOutlinedComboBox}"
                                          materialDesign:HintAssist.Hint="Select request (patient / test)"/>

                                <!-- Result date -->
                                <DatePicker Grid.Row="2" Grid.ColumnSpan="2"
                                            Style="{DynamicResource MaterialDesignFilledDatePicker}"
                                            materialDesign:HintAssist.Hint="Result date"
                                            SelectedDate="{Binding ResultEntry.ResultDate}"/>

                                <!-- Result text -->
                                <TextBox Grid.Row="3" Grid.ColumnSpan="2"
                                         AcceptsReturn="True" TextWrapping="Wrap"
                                         VerticalAlignment="Stretch" MinHeight="220"
                                         Style="{DynamicResource MaterialDesignFilledTextBox}"
                                         materialDesign:HintAssist.Hint="Result data"
                                         Text="{Binding ResultEntry.ResultData}"/>

                                <StackPanel Grid.Row="4" Grid.ColumnSpan="2"
                                            Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="Cancel" Style="{DynamicResource MaterialDesignFlatButton}"/>
                                    <Button Content="Save Result" Style="{DynamicResource MaterialDesignRaisedButton}" Margin="8,0,0,0"
                                            Command="{Binding SaveResultCommand}"/>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                    </materialDesign:Card>
                </DockPanel>
</UserControl>
